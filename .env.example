# Anthropic API Key (required)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Convex (auto-generated by `npx convex dev`)
CONVEX_DEPLOYMENT=dev:your-deployment
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# PostHog Analytics (optional)
# Get from: https://app.posthog.com
NEXT_PUBLIC_POSTHOG_KEY=phc_your-project-key
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ==================================
# Convex Auth Environment Variables
# (set these via `npx convex env set` or in Convex Dashboard)
# ==================================

# SITE_URL - Your app's URL (for OAuth redirects)
# For local development: http://localhost:3000
# For production: https://your-app.vercel.app
# Set via: npx convex env set SITE_URL http://localhost:3000

# JWT_PRIVATE_KEY and JWKS - Generated by running `npx @convex-dev/auth`
# Or generate manually with the script in docs and set via:
# npx convex env set JWT_PRIVATE_KEY "your-private-key"
# npx convex env set JWKS '{"keys":[...]}'

# GitHub OAuth (optional)
# Create at: https://github.com/settings/applications/new
# Callback URL: https://your-deployment.convex.site/api/auth/callback/github
# Set via:
# npx convex env set AUTH_GITHUB_ID your-github-client-id
# npx convex env set AUTH_GITHUB_SECRET your-github-client-secret

# Google OAuth (optional)
# Create at: https://console.cloud.google.com/auth/overview
# Redirect URI: https://your-deployment.convex.site/api/auth/callback/google
# Set via:
# npx convex env set AUTH_GOOGLE_ID your-google-client-id
# npx convex env set AUTH_GOOGLE_SECRET your-google-client-secret
